#!/bin/bash
#
# create a vm to simulate semaphoreci environment locally.
# currently assumes you already have ccloudvm installed and
# the semaphore workload copied to ~/.ccloudvm/workloads
#

ccloudvm create semaphore

